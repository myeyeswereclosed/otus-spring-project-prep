server:
  port: 8085
  servlet:
    context-path: /sms-service

spring:
  datasource:
    platform: postgres
    url: jdbc:postgresql://localhost:5432/sms_code_base
    username: postgres
    password: testpassword
    driverClassName: org.postgresql.Driver
    initialization-mode: always
  jpa:
    show-sql: true
sms-code:
  expirationTimeInSeconds: 90
  waitingInterval: 60 #seconds
  countRequestsInterval: 300 #seconds
  requestsNumber: 3


