spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/myeyeswereclosed/otus-spring-config/
#          searchPaths: service-discovery, comment-service, book-service
          searchPaths: '{application}'
server:
  port: 4000